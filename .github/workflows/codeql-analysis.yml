name: "CodeQL"

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  schedule:
    - cron: '6 17 * * 3'

jobs:
  analyze:
    uses: voxpelli/ghatemplates/.github/workflows/codeql-analysis.yml@main
